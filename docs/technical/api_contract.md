# API Contract

**Purpose:** Keeps frontend and backend aligned. Single source for endpoints and schemas.

- Base URL and versioning
- Authentication requirements
- Endpoints (method, path, description)
- Request schema per endpoint
- Response schema per endpoint
- Unified error schema
- Rate limiting rules

# User Stories

**Purpose:** Features from the user's perspective. Use when you have 20+ distinct user flows (otherwise keep in prd.md).

Format:

```
As a [user],
I want to [action],
So that [benefit].
```

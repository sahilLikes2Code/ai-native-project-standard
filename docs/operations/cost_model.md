# Cost Model

**Purpose:** Prevents AI cost blow-up. Plan before the first big bill.

- Estimated cost per request type
- Token usage per call
- Worst-case scaling
- Monthly budget threshold
- Alerts and auto-cutoffs
- Model fallback when over threshold

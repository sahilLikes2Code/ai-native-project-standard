# Context Map

**Purpose:** What context the AI gets per request (and what it does not). Stops wrong assumptions.

- Persistent memory (survives across sessions)
- Session memory (current session only)
- User state (per request)
- Dynamic context (injected at runtime)
- What is explicitly NOT included and why

# AI Contract

**Purpose:** How the app talks to AI models. Output shape, retries, fallbacks. Key for preventing production incidents.

- System prompt(s)
- Prompt structure and formatting rules
- Output JSON schema
- Tool usage format
- Error handling format
- Token limits per call type
- Retry strategy
- Model fallback rules
- Prompt changelog (version, date, change, performance impact)

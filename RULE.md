# Editor AI rules (template)

**Purpose:** Copy this into `.cursor/rules/` or use as `.cursorrules` at root. Tells the AI coding assistant how to write code in this repo.

- Stack conventions and preferred libraries
- Forbidden libraries or patterns
- Folder structure and naming rules
- Code style rules
- What to always do and never do
- Testing expectations

This file is **not** the same as `agents/agents.md` (which defines in-app runtime agent behavior).

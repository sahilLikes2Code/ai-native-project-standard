# Agents

**Purpose:** In-app AI agent behavior at runtime. (For how the coding assistant edits this repo, see `.cursor/rules` or RULE.md.)

- Agent roles and responsibilities
- Capabilities
- Hard boundaries (what the agent must never do)
- Allowed tools (input/output schema per tool)
- Memory usage rules (persistent vs session)
- Access rules per tool
